---
include:
  # Minimal diagnostic build: no OLED and no Studio
  - board: nice_nano_v2
    shield: custom_keyboard
    cmake-args: -DCONFIG_ZMK_DISPLAY=n
    artifact-name: custom_keyboard_diagnostic

  # Complete firmware
  - board: nice_nano_v2
    shield: custom_keyboard
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y
    artifact-name: custom_keyboard

  - board: nice_nano_v2
    shield: settings_reset
    artifact-name: settings_reset
 
  # OLED diagnostic build: OLED enabled, Studio disabled, USB logging enabled
  - board: nice_nano_v2
    shield: custom_keyboard
    snippet: zmk-usb-logging
    artifact-name: custom_keyboard_oled_log